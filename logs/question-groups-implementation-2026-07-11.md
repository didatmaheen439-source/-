# 题组管理实现记录 2026-07-11

## 实现范围

- 题组列表、新建/编辑、详情、复制、预校验和提交审核页面。
- 题组 Mock Store、API、乐观锁、审核任务和发布状态同步。
- 学习路径动态题组引用。
- 模考试卷题组批量添加、顺序展开、题目快照和来源追踪。
- 学习路径与模考试卷引用影响聚合。
- 导航、审核中心、运营数据和自动化测试联动。

## 验证

- `npm run test`：通过，14 个测试文件、74 条测试。
- `npm run lint`：通过，Biome 328 个文件无问题，`tsc --noEmit` 通过。
- `npx antd lint ./src`：通过，扫描 293 个文件无问题。
- `npm run build`：通过，生成 `admin-web/dist/`，共 59 个资源文件。
- HTTP 页面检查：`GET /content/question-groups` 返回 200。
- API smoke：登录、题组列表、详情、影响范围和模考题组展开均成功。
- 状态流 smoke：`draft -> pending_review -> approved -> pending_publish -> published` 全部成功，审核任务和题组最终状态均为 `published`。
- 模考展开：四级阅读核心题组展开为 3 道有序题目快照，每道均保留来源题组 ID、名称和版本。
- 浏览器自动化：in-app browser 连接本地页面持续超时，未完成截图和点击级视觉验收；页面构建、HTTP、API 和状态流已验证。

## 已知边界

- 当前为内存 Mock，开发服务器重启后恢复种子数据。
- 本阶段只展示下架影响，不执行下游自动迁移或级联下架。
- 浏览器视觉验收仍需在浏览器控制连接稳定后补做。
