# 模考试卷配置 MVP 实施日志

日期：2026-07-09

## 基线

- Git 根目录：`/Users/inn/Desktop/Workspace/projects/project-004-过级搭子后台管理系统`
- 开工分支：`codex/ai-coach-strategy-mvp`
- 开工 Commit：`114b0d1`
- 开工状态：工作区干净

## 实际实现

- 新增根目录 `PRODUCT.md`，仅归纳现有后台定位、用户、模块、原则和阶段边界。
- 将题库数据与引用查询从 `mock/user.ts` 提取至 `admin-web/mock/contentQuestionStore.ts`，原题库 API 继续复用同一数据源。
- 新增模考试卷 Store、Mock API、页面本地类型与服务层。
- 实现试卷列表、新建、编辑、详情、CET4/CET6 标准模板、四分区编排、题目选择 Drawer、顺序调整、预校验、复制草稿、版本差异和聚合统计。
- 审核发布中心接入 `objectType=mock_exam`，发布前复验并同步试卷状态。
- 工作台、运营数据和用户学习记录接入模考摘要。
- 权限矩阵增加教研、数据分析和只读审计的模考权限。
- 清理 `npx antd lint ./src` 报出的既有 Ant Design 6 弃用属性，不改变页面业务逻辑。

## API 闭环验证

已执行一轮 20 项 API 冒烟检查，结果通过：

- 列表、详情、混合来源查询、创建和编辑。
- `dataVersion` 冲突返回 409。
- error 阻断提交，模拟服务异常返回 500。
- warning 未确认阻断，确认后可提交。
- 提交审核生成模考任务。
- 提交人自审返回 403，另一教研账号可审核、安排发布和发布。
- 重复发布幂等，业务对象状态同步。
- 下架原因必填，已发布试卷可复制为新草稿。
- 聚合统计不包含用户 ID、答案或原文。
- 数据分析只读、内容运营无权限。
- 运营数据模考来源为 `formal=true`，工作台包含模考摘要。

## 浏览器验证

已执行：

- 1280x900：列表、新建、编辑、详情、分区编排、题目选择 Drawer、结果统计、审核发布、工作台、运营数据和用户学习记录可访问。
- 1024x768：试卷列表正常，表格在自身容器内横向滚动，页面无顶层横向溢出。
- 390x844：试卷列表无白屏、无 403，页面无顶层横向溢出。
- 写作分区可从 Drawer 选择同考试类型已发布写作题目，选择后分区题数和表格同步更新。
- 客服用户详情的学习记录显示试卷 ID、版本和聚合结果摘要，不显示逐题答案。
- 上述模考页面控制台未出现应用 error、warning 或 Ant Design deprecated 提示。

## 发现与修复

1. 首次执行 `npm run test -- mockExamStore` 时，测试文件位于 `mock/`，不符合项目 Vitest 的 `src/**` 扫描规则；命令退出码为 0，但提示未发现测试。测试移入 `src/pages/mock-exam/papers/` 后，实际执行 6 项测试并通过。
2. 修改角色权限后，已运行开发服务器仍使用旧 mock 模块缓存；重启 `npm run start` 后新权限生效。
3. 现有审核发布幂等分支只覆盖题库、学习路径、AI 和写译；新增 `mock_exam` 后首次重复发布检查失败。已把模考对象加入共享幂等分支并重新完成 API 闭环。
4. 开发服务器仍会报告 `127.0.0.1:8001 ECONNREFUSED`，属于现有本地代理非阻断问题；8000 页面和 Mock API 正常。

## 已执行命令

- `git diff --check`：通过，无空白错误。
- `npm run tsc`：通过。
- `npm run lint`：通过，Biome 检查 315 个文件，TypeScript 检查通过。
- `npx antd lint ./src`：通过，扫描 280 个文件，无问题。
- `npm run test -- mockExamStore`：通过，1 个测试文件、6 项测试。
- `npm run build`：通过，生成 53 个资源文件和全部模考试卷路由静态入口。
- `npm run start`：构建后重新启动成功，主服务监听 `http://localhost:8000`。
- 构建后 HTTP 冒烟：登录接口、`/mock-exam/papers` 和 `/api/mock-exam/papers` 均返回 200。

## 构建后验证限制

- 构建后尝试重新连接浏览器进行第二轮列表和详情回归时，浏览器控制连续超时并重置。
- 未把该轮浏览器交互写为已完成；构建前完整浏览器验收已通过，构建后以构建成功和页面/API HTTP 200 作为补充证据。
- 阶段 Commit 在文档与差异审查完成后执行。

## 风险

- Mock 数据和状态重启后重置。
- 当前题型和题量只用于后台配置闭环验证，不能作为正式 CET 试卷内容。
- 真实后端阶段必须在服务端重新实现权限、乐观锁、发布复验、审计和聚合脱敏。
